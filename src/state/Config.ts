import { Vector3 } from "three";

const SCENE = {
  cameraPosition: new Vector3(0, 2, 3.5),
  ambientIntensity: 0.5,
  lightPosition: new Vector3(3, 3, 3),
  pointIntensity: 20,
  baseRadius: 1,
  baseHeight: 0.25,
  ROTATION_RATE: 0.25,
};

export { SCENE };
